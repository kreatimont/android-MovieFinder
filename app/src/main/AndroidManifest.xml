<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nadto.cinematograph">

    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
            <meta-data android:name="android.app.searchable"
                android:resource="@xml/searchable" />

        </activity>

        <activity
            android:launchMode="singleTop"
            android:noHistory="true"
            android:name=".activity.MovieDetailedActivity"
            android:label="@string/title_activity_detailed"
            android:parentActivityName=".activity.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.nadto.cinematograph.activity.MainActivity" />
        </activity>

        <activity
            android:launchMode="singleTop"
            android:noHistory="true"
            android:name=".activity.TvDetailedActivity"
            android:label="@string/title_activity_detailed"
            android:parentActivityName=".activity.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.nadto.cinematograph.activity.MainActivity" />
        </activity>

        <activity
            android:launchMode="singleTop"
            android:noHistory="true"
            android:name=".activity.PersonDetailedActivity"
            android:label="Person detailed"
            android:parentActivityName=".activity.MovieDetailedActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.nadto.cinematograph.activity.MovieDetailedActivity" />
        </activity>

    </application>

</manifest>